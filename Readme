This script is a barebones implementation doing auto transfers based on a sleep timer. Addional updates will include better documentation, log statistics to file, and a built in 
server maintenence system that allows an auto safe shutdown of negn server and reboot of system services. 
